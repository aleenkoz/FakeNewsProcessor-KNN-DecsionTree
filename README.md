# FakeNewsProcessor-KNN-DecsionTree
To solve the fake4 news problem, two algorithms are used: Decision Tree and K-Nearest Neighbors (KNN). The Decision Tree creates classification rules by splitting the data based on  important features. In contrast, KNN compares a new article with similar articles in the dataset and  assigns the most common label among its nearest neighbors.
